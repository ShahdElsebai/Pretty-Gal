import styled from 'styled-components'

const Header = () => (
  <StyledHeader>Pretty Gal (Raleway)</StyledHeader>
)

const StyledHeader = styled.p`
  font-family: Raleway, serif;
`

export default Header
